# 免费模型配置操作手册

本文档说明如何维护 QNote 的免费模型远程配置文件 `free_models.json`。

## 概述

- **远程仓库**：https://github.com/favoroo/QNote
- **配置文件**：仓库根目录的 `free_models.json`
- **获取方式**：应用通过 jsDelivr CDN（国内友好）拉取，失败回退 GitHub raw
- **apikey 安全**：apikey 经混淆处理后存入 JSON，不直接暴露明文

## 一、添加新模型

### 1. 编辑生成脚本

打开 `tool/generate_free_models.dart`，在 `models` 列表中新增一项：

```dart
final models = <Map<String, dynamic>>[
  // ... 已有模型 ...

  {
    'id': 'new-model-id',              // 唯一标识，英文短横线格式
    'displayName': '显示名称',           // 用户看到的名称
    'provider': 'openai',               // 协议类型：'openai' 或 'gemini'
    'baseUrl': 'https://api.example.com/v1',  // 接口地址（gemini 可留空）
    'modelName': 'model-name-1.0',      // 实际调用的模型 ID
    'apiKey': 'sk-your-api-key-here',   // 明文 apikey
    'authType': 'bearer',               // 鉴权方式：'bearer' 或 'query'
    'priority': 4,                      // 切换优先级（数字越小越优先）
  },
];
```

**字段说明**：

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | 唯一标识符，使用英文短横线格式，如 `sensenova-flash-lite` |
| `displayName` | 是 | 在 UI 中展示的中文名称 |
| `provider` | 是 | 协议类型：`openai`（OpenAI 兼容接口）或 `gemini`（Google Gemini） |
| `baseUrl` | 是 | API 接口地址。Gemini 模型可留空（自动使用官方接口） |
| `modelName` | 是 | 实际调用时传给 API 的模型标识符 |
| `apiKey` | 是 | 明文 apikey，脚本会自动混淆处理 |
| `authType` | 否 | 鉴权方式，默认 `bearer`。Gemini 用 `query` |
| `priority` | 否 | 自动切换优先级，默认 `0`。数字越小越优先尝试 |

**provider 与 authType 对照表**：

| provider | authType | 说明 |
|----------|----------|------|
| `openai` | `bearer` | 大多数 OpenAI 兼容接口（SenseNova、Agnes 等） |
| `gemini` | `query` | Google Gemini 系列（通过 URL 参数传递 key） |

### 2. 运行脚本生成

在项目根目录执行：

```bash
dart run tool/generate_free_models.dart
```

输出示例：

```
已生成 free_models.json

请将此文件推送到 GitHub 仓库 https://github.com/favoroo/QNote 的 main 分支根目录。

=== 验证混淆/解混淆 ===
sensenova-flash-lite: ✓ (原文长度=35, 混淆后长度=48)
agnes-flash: ✓ (原文长度=51, 混淆后长度=68)
gemini-flash-lite: ✓ (原文长度=39, 混淆后长度=52)
gemma: ✓ (原文长度=39, 混淆后长度=52)
```

所有行显示 `✓` 表示混淆/解混淆验证通过。

### 3. 验证生成的文件

查看生成的 `free_models.json`，确认结构正确：

```json
{
  "version": "1.0",
  "updatedAt": "2026-06-22T14:55:57.856292Z",
  "models": [
    {
      "id": "sensenova-flash-lite",
      "displayName": "SenseNova Flash Lite",
      "provider": "openai",
      "baseUrl": "https://token.sensenova.cn/v1",
      "modelName": "sensenova-6.7-flash-lite",
      "obfuscatedApiKey": "=UjFaciDDARAMFwNDYAFKsSCkkQLWQzBUgicClUQnESQCVgA",
      "authType": "bearer",
      "priority": 0
    }
  ]
}
```

> **注意**：`apiKey` 字段在 JSON 中为 `obfuscatedApiKey`（混淆后的值），明文不会出现在文件中。

## 二、推送到 GitHub

### 方式一：使用脚本一键推送（推荐）

运行以下命令即可完成推送：

```bash
cd %TEMP%\qnote_free_models
git add free_models.json
git commit -m "chore: update free models config"
git push origin main
```

### 方式二：手动推送

```bash
# 1. 进入临时仓库目录
cd %TEMP%\qnote_free_models

# 2. 复制最新的 free_models.json 到此目录
copy "c:\01Code\qnote_flutter\free_models.json" .

# 3. 提交并推送
git add free_models.json
git commit -m "chore: update free models config"
git push origin main
```

### 推送后验证

确认文件可访问：

- **jsDelivr（国内推荐）**：https://cdn.jsdelivr.net/gh/favoroo/QNote@main/free_models.json
- **GitHub raw**：https://raw.githubusercontent.com/favoroo/QNote/main/free_models.json

> **缓存提示**：jsDelivr 有 CDN 缓存，首次推送后可能需要等待几分钟才能生效。如需立即生效，可在 URL 后加时间戳参数绕过缓存。

## 三、填写 API Key

### 获取 API Key 的途径

| 模型厂商 | 获取地址 | 注册要求 |
|----------|----------|----------|
| SenseNova（商汤） | https://platform.sensenova.cn | 手机号注册，免费额度 |
| Agnes AI | https://apihub.agnes-ai.com | 注册即可，免费额度 |
| Google Gemini | https://aistudio.google.com/apikey | Google 账号，免费额度 |

### 填写规则

1. **直接填入明文**：在 `tool/generate_free_models.dart` 中 `apiKey` 字段填入原始 key
2. **无需手动混淆**：脚本会自动执行 XOR + Base64 + 翻转混淆
3. **不要把明文 key 写到 JSON 里**：JSON 文件中只存储混淆后的值

### 示例

```dart
// 正确 ✅ — 填明文
'apiKey': 'sk-5DxsypDwruQsrdKmNfgYlUg9rsbzNuxj',

// 错误 ❌ — 不要填混淆后的值
'apiKey': '=UjFaciDDARAMFwNDYAFKsSCkkQLWQzBUgicClUQnESQCVgA',
```

## 四、修改现有模型

### 修改 apikey

如果某个模型的 apikey 过期了：

1. 打开 `tool/generate_free_models.dart`
2. 找到对应模型，替换 `apiKey` 为新的明文 key
3. 重新运行 `dart run tool/generate_free_models.dart`
4. 推送到 GitHub

### 修改 priority（调整切换顺序）

priority 数字越小的模型会被优先尝试。例如想让 Agnes 排第一：

```dart
// 修改前
{ 'id': 'agnes-flash', 'priority': 1 },

// 修改后
{ 'id': 'agnes-flash', 'priority': 0 },
```

同时把其他模型的 priority 往后调整，避免重复。

### 移除模型

从 `models` 列表中删除对应项即可。重新生成并推送后，应用端下次"更新"就不会再看到该模型。

## 五、用户端操作

### 更新免费模型列表

1. 打开 QNote → 设置 → AI 配置
2. 找到页面顶部的「免费模型」卡片
3. 点击右上角「更新」按钮
4. 等待拉取成功提示

### 选择主模型

1. 在「免费模型」卡片的「主模型」下拉框中选择
2. 选择「自动选择（按优先级）」则按 priority 从小到大依次尝试
3. 选择具体模型则优先使用该模型，失败后仍自动切换

### 启用免费模式

1. 在同一页面找到「角色绑定」区域
2. 「提问助手」或「时间轴智能提取」的下拉框中选择「🎁 免费模型（自动切换）」
3. 之后该角色的 AI 调用就会使用免费模型

## 六、常见问题

### Q: 推送后应用中点击"更新"仍然 404？

jsDelivr 缓存延迟导致。解决方案：
- 等待 5~10 分钟后重试
- 或访问 jsDelivr URL 确认是否返回内容

### Q: 国内网络无法访问 GitHub raw？

代码中已实现回退策略：**jsDelivr 优先 → GitHub raw 回退**。只要 jsDelivr 可访问就能正常工作。如果两个都不可用，应用会使用本地缓存的旧版本。

### Q: apikey 泄露了怎么办？

1. 在对应厂商平台**重新生成新的 apikey**
2. 修改 `tool/generate_free_models.dart` 中的 key
3. 重新生成并推送
4. 通知用户点击"更新"

### Q: 可以同时存在多少个免费模型？

无硬性限制，但建议控制在 10 个以内，过多会影响用户体验和切换效率。

## 七、安全提醒

- `free_models.json` 中的 apikey 经过简单混淆（非加密），反编译可还原
- 仅适用于**价值有限的免费 apikey**
- 请勿将付费/高价值 apikey 放入此文件
- GitHub 密码等敏感凭据**绝不要**写入任何代码或配置文件
