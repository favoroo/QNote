- **Dual-Architecture Coexistence**: The root module orchestrates two distinct implementation layers: a legacy React/TypeScript web app (`01旧架构数据/`) serving as a feature reference or migration source, and the primary Flutter application (`lib/`, `android/`, `web/`).
- **Platform-Native Integration Bridge**: Establishes a bidirectional communication channel between the Flutter runtime and native Android components (Widgets/Quick Record) via `MethodChannel('com.appone.qnote_flutter/widgets')`, enabling deep OS-level integration for quick-capture workflows.
- **Unified Initialization Lifecycle**: Centralizes cross-cutting concerns in `main.dart`, enforcing a strict startup sequence that initializes logging, date formatting, platform-specific database factories, and background synchronization schedulers before the UI layer is mounted.