# Flutter APK 构建与安装手册
## 常用命令

```shellscript
flutter run -d chrome --web-port=8080
```

## 安卓构建与安装命令

```shellscript
# 构建成功后再安装到已连接设备
flutter build apk && adb install -r build/app/outputs/flutter-apk/app-release.apk
```


## iOS构建与运行命令

```shellscript
# 构建并直接运行到已连接的真机
flutter run -d 守望者的iPhone
```

```
flutter run --release -d 守望者的iPhone
```

```shellscript
# 查看可用设备列表
flutter devices
```