# Flutter APK 构建与安装手册

<br />

<br />

## 一键构建并安装命令

**方式一：分步执行（推荐）**

```powershell
flutter build apk; adb install -r build\app\outputs\flutter-apk\app-release.apk
```

**方式二：条件执行（构建成功后才安装）**

```powershell
flutter build apk; if ($LASTEXITCODE -eq 0) { adb install -r build\app\outputs\flutter-apk\app-release.apk }
```

***

## 分步操作

### 1. 构建 APK

```powershell
flutter build apk
```

**输出位置：** `build\app\outputs\flutter-apk\app-release.apk`

***

### 2. 查看已连接设备

```powershell
adb devices
```

***

### 3. 安装到手机

**单设备时：**

```powershell
adb install -r build\app\outputs\flutter-apk\app-release.apk
```

**多设备时（指定设备ID）：**

```powershell
adb -s <设备ID> install -r build\app\outputs\flutter-apk\app-release.apk
```

***

## 常用命令速查

| 命令                                  | 说明                 |
| ----------------------------------- | ------------------ |
| `flutter build apk`                 | 构建 Release APK     |
| `flutter build apk --debug`         | 构建 Debug APK       |
| `flutter build apk --split-per-abi` | 按 CPU 架构分包构建（体积更小） |
| `adb devices`                       | 查看已连接设备            |
| `adb install -r <apk路径>`            | 安装 APK（-r 表示覆盖安装）  |
| `adb uninstall <包名>`                | 卸载应用               |

***

## 构建输出路径

```
build\app\outputs\flutter-apk\
├── app-release.apk        # Release 版本
└── app-debug.apk          # Debug 版本
```

***

## 注意事项

1. **手机需开启 USB 调试模式**
2. **手机需连接电脑并授权调试**
3. **APK 签名问题**：首次安装可能需要先卸载旧版本
4. <br />
5. <br />

## 运行应用

```shellscript
flutter run -d chrome --web-port=8080
```

